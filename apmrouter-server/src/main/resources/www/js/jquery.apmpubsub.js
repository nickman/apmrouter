// $('#resources').bind('customEvent', function (event, data) { console.info('this: %o, event: %o, data: %o', this, event, data); } )
// responds to 
// $.event.trigger('customEvent', [['Hello', 'World']]);

(function($){
	$.fn.apmpubsub = function(options) {
		
	}
	$.fn.apmsubscribe = function(metricUri, callbacks) {
		
		
		//return this.css('color','blue');
	};
	
	
	
})(jQuery);