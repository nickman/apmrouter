/**
 * Monitoring script to monitor win stats
 * Whitehead, Oct 1, 2012
 */
	
	/*
	tracer.traceGauge(procStats.length, "ProcessCount", "platform=winServer");
	for(var i = 0, m = procStats.length; i < m; i++) {
		tracer.traceGauge(procStats[i].handleCount, "HandleCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].threadCount, "ThreadCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceDeltaGauge(procStats[i].kernelModeTime, "KernelModeTime", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceDeltaGauge(procStats[i].userModeTime, "UserModeTime", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].maxWorkingSetSize, "MaxWorkingSetSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].minWorkingSetSize, "MinWorkingSetSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].peakWorkingSetSize, "PeakWorkingSetSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].workingSetSize, "WorkingSetSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].virtualSize, "VirtualSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].peakVirtualSize, "PeakVirtualSize", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].pageFaults, "PageFaults", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].pageFileUsage, "PageFileUsage", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].peakPageFileUsage, "PeakPageFileUsage", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].privatePageCount, "PrivatePageCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].readOperationCount, "ReadOperationCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].writeOperationCount, "WriteOperationCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].otherOperationCount, "OtherOperationCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].readTransferCount, "ReadTransferCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].writeTransferCount, "WriteTransferCount", "platform=winServer", "instance=" + procStats[i].fileName);
		tracer.traceGauge(procStats[i].otherTransferCount, "OtherTransferCount", "platform=winServer", "instance=" + procStats[i].fileName);
	}
	*/
} 
 

